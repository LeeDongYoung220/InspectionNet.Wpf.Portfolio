<UserControl x:Class="InspectionNet.Wpf.PocProject.Views.PocLightView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:InspectionNet.Wpf.PocProject.ViewModels"
             d:DataContext="{d:DesignInstance vm:PocLightViewModel}"
             DataContext="{Binding Light, Source={StaticResource ViewModelLocator}}"
             xmlns:local="clr-namespace:InspectionNet.Wpf.PocProject.Views"
             xmlns:models="clr-namespace:InspectionNet.Wpf.PocProject.Models"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="350"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignPaperButton}">
                <Setter Property="Margin" Value="5"/>
            </Style>
            <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Padding" Value="16,8"/>
            </Style>
        </Grid.Resources>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <UniformGrid Grid.Column="0" Rows="7">
                <Button Content="Search Port" Command="{Binding SearchPortCommand}"/>
                <ComboBox materialDesign:HintAssist.Hint="PortName" ItemsSource="{Binding PortNames}" SelectedItem="{Binding LightController.PortName}"/>
                <ComboBox materialDesign:HintAssist.Hint="BaudRate" ItemsSource="{Binding BaudRates}" SelectedItem="{Binding LightController.BaudRate}"/>
                <ComboBox materialDesign:HintAssist.Hint="DateBits" ItemsSource="{Binding DataBits}" SelectedItem="{Binding LightController.DataBits}"/>
                <ComboBox materialDesign:HintAssist.Hint="Parity" ItemsSource="{Binding Parities}" SelectedItem="{Binding LightController.Parity}"/>
                <ComboBox materialDesign:HintAssist.Hint="StopBits" ItemsSource="{Binding StopBits}" SelectedItem="{Binding LightController.StopBits}"/>
                <UniformGrid Columns="2">
                    <Button Content="Open" Command="{Binding OpenCommand}"/>
                    <Button Content="Close" Command="{Binding CloseCommand}"/>
                </UniformGrid>
            </UniformGrid>
        </Grid>
        
        
        <!--<ListView Grid.Row="1" HorizontalContentAlignment="Stretch" ItemsSource="{Binding Lights}" ScrollViewer.CanContentScroll="False">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:Light}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" Margin="5,0" Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
                        <Slider Grid.Column="1" Value="{Binding Value}" Margin="10,0" VerticalAlignment="Center" Minimum="0" Maximum="255" TickFrequency="1" Style="{StaticResource MaterialDesign3.MaterialDesignDiscreteSlider}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
    </Grid>
</UserControl>
