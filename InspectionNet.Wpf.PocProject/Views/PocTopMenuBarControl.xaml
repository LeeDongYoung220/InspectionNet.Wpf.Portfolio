<UserControl x:Class="InspectionNet.Wpf.PocProject.Views.PocTopMenuBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:InspectionNet.Wpf.PocProject.ViewModels"
             d:DataContext="{d:DesignInstance vm:PocMenuBarViewModel}"
             DataContext="{Binding MenuBar, Source={StaticResource ViewModelLocator}}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InspectionNet.Wpf.PocProject.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="1000">
    <Grid>
        <ListBox SelectedIndex="{Binding SelectedMenuIndex}" Style="{StaticResource MaterialDesign3.NavigationBarListBox}">
            <ListBoxItem>
                <ListBoxItem.Style>
                    <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource MaterialDesign3.NavigationBarPrimaryListBoxItem}">
                        <Setter Property="materialDesign:NavigationBarAssist.SelectedIcon" Value="Home" />
                        <Setter Property="materialDesign:NavigationBarAssist.UnselectedIcon" Value="HomeOutline" />
                    </Style>
                </ListBoxItem.Style>
                <TextBlock Text="HOME" />
            </ListBoxItem>
            <ListBoxItem>
                <ListBoxItem.Style>
                    <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource MaterialDesign3.NavigationBarPrimaryListBoxItem}">
                        <Setter Property="materialDesign:NavigationBarAssist.SelectedIcon" Value="Video" />
                        <Setter Property="materialDesign:NavigationBarAssist.UnselectedIcon" Value="VideoOutline" />
                    </Style>
                </ListBoxItem.Style>
                <TextBlock Text="CAMERA" />
            </ListBoxItem>
            <ListBoxItem>
                <ListBoxItem.Style>
                    <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource MaterialDesign3.NavigationBarPrimaryListBoxItem}">
                        <Setter Property="materialDesign:NavigationBarAssist.SelectedIcon" Value="Lightbulb" />
                        <Setter Property="materialDesign:NavigationBarAssist.UnselectedIcon" Value="LightbulbOutline" />
                    </Style>
                </ListBoxItem.Style>
                <TextBlock Text="LIGHT" />
            </ListBoxItem>
            <ListBoxItem>
                <ListBoxItem.Style>
                    <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource MaterialDesign3.NavigationBarPrimaryListBoxItem}">
                        <Setter Property="materialDesign:NavigationBarAssist.SelectedIcon" Value="Settings" />
                        <Setter Property="materialDesign:NavigationBarAssist.UnselectedIcon" Value="SettingsOutline" />
                    </Style>
                </ListBoxItem.Style>
                <TextBlock Text="CONFIG" />
            </ListBoxItem>
            <ListBoxItem>
                <ListBoxItem.Style>
                    <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource MaterialDesign3.NavigationBarPrimaryListBoxItem}">
                        <Setter Property="materialDesign:NavigationBarAssist.SelectedIcon" Value="Toolbox" />
                        <Setter Property="materialDesign:NavigationBarAssist.UnselectedIcon" Value="ToolboxOutline" />
                    </Style>
                </ListBoxItem.Style>
                <TextBlock Text="TOOL" />
            </ListBoxItem>
            <ListBoxItem>
                <ListBoxItem.Style>
                    <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource MaterialDesign3.NavigationBarPrimaryListBoxItem}">
                        <Setter Property="materialDesign:NavigationBarAssist.SelectedIcon" Value="LanConnect" />
                        <Setter Property="materialDesign:NavigationBarAssist.UnselectedIcon" Value="LanDisconnect" />
                    </Style>
                </ListBoxItem.Style>
                <TextBlock Text="COMM." />
            </ListBoxItem>
        </ListBox>
    </Grid>
</UserControl>
