<UserControl x:Class="InspectionNet.Wpf.PocProject.Views.PocToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             d:DataContext="{d:DesignInstance vm:PocToolViewModel}"
             DataContext="{Binding Tool, Source={StaticResource ViewModelLocator}}"
             xmlns:vm="clr-namespace:InspectionNet.Wpf.PocProject.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InspectionNet.Wpf.PocProject.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cvt="clr-namespace:InspectionNet.Wpf.PocProject.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignPaperButton}">
                <Setter Property="Margin" Value="5,0"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <UniformGrid Grid.Row="0" Columns="5">
            <Button Command="{Binding SelectDirectoryCommand}" Content="SelectDirectory"/>
            <Button Command="{Binding ExecuteAllCommand}" Content="ExecuteAll"/>
        </UniformGrid>

        

        <Grid Grid.Row="1">
            <Grid.Resources>
                <cvt:BoolVisibilityConverter x:Key="BoolVisibilityConverter"/>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="3"/>
                <ColumnDefinition Width="500"/>
            </Grid.ColumnDefinitions>

            <ContentControl Grid.Column="0" Content="{Binding LaonDisplay}"/>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <TextBlock Text="Use Grab Image" VerticalAlignment="Center" Margin="5,0"/>
                    <ToggleButton IsChecked="{Binding UseGrabImage}" Margin="5,0"/>
                    <Button Content="Input Image (Load from file)" Command="{Binding LoadImageFileCommand}"/>
                </StackPanel>

                <TabControl Grid.Row="1">
                    <TabItem Header="Rule">
                        <local:PocRuleToolView/>
                    </TabItem>

                    <TabItem Header="AI">
                        <local:PocAIToolView/>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="Save Overlay Image" VerticalAlignment="Center" Margin="5,0"/>
            <ToggleButton IsChecked="{Binding IsSaveOverlayImage}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
