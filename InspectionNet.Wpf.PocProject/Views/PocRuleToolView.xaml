<UserControl x:Class="InspectionNet.Wpf.PocProject.Views.PocRuleToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:InspectionNet.Wpf.PocProject.ViewModels"
             d:DataContext="{d:DesignInstance vm:PocRuleToolViewModel}"
             DataContext="{Binding RuleTool, Source={StaticResource ViewModelLocator}}"
             xmlns:local="clr-namespace:InspectionNet.Wpf.PocProject.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Column="1" Content="Calibration" Command="{Binding OpenCalibToolCommand}" HorizontalAlignment="Left"/>
        <Grid Grid.RowSpan="3" DataContext="{Binding CurrentToolGroupParameters}">
            <Grid.Resources>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="TextAlignment" Value="Right"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="5,0"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Calibration Mode"/>
            <TextBlock Grid.Row="1" Text="Save Origin Image"/>
            <TextBlock Grid.Row="2" Text="CSV Save"/>
            <ToggleButton Grid.Row="0" Grid.Column="1" IsChecked="{Binding IsCalibrationMode}"/>
            <ToggleButton Grid.Row="1" Grid.Column="1" IsChecked="{Binding IsSaveOriginImage}"/>
            <ToggleButton Grid.Row="2" Grid.Column="1" IsChecked="{Binding IsSaveCsv}"/>
        </Grid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
            <Button Content="Tool Block" Command="{Binding OpenToolBlockCommand}" Margin="5,0"/>
            <Button Content="Run" Command="{Binding RuleRunCommand}"/>
        </StackPanel>

    </Grid>
</UserControl>
