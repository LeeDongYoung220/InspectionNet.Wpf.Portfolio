<UserControl xmlns:UIElements="clr-namespace:InspectionNet.Wpf.PocProject.UIElements"  x:Class="InspectionNet.Wpf.PocProject.Views.PocHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:InspectionNet.Wpf.PocProject.ViewModels"
             d:DataContext="{d:DesignInstance vm:PocHomeViewModel}"
             DataContext="{Binding Home, Source={StaticResource ViewModelLocator}}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InspectionNet.Wpf.PocProject.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="200"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" HorizontalAlignment="Stretch">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                    <Setter Property="Margin" Value="10"/>
                </Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </StackPanel.Resources>
            <Grid>
                <Grid.Resources>
                    <Style TargetType="{x:Type Ellipse}">
                        <Setter Property="Width" Value="15"/>
                        <Setter Property="Height" Value="15"/>
                    </Style>
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Camera"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="Tool"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Motion"/>
                <UIElements:StatusIndicatorControl Grid.Row="1" Grid.Column="0" StatusValue="{Binding IsInitializedCamera}"/>
                <UIElements:StatusIndicatorControl Grid.Row="1" Grid.Column="1" StatusValue="{Binding IsInitializedTool}"/>
                <UIElements:StatusIndicatorControl Grid.Row="1" Grid.Column="2" StatusValue="{Binding IsInitializedMotion}"/>
            </Grid>
            <Button Content="Initialize" Command="{Binding InitComponentCommand}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Motion Init" Margin="20,0"/>
                <ToggleButton IsChecked="{Binding IsInitMotion}"/>
            </StackPanel>
            <Button Content="Align" Command="{Binding AlignSequenceCommand}"/>
            <Button Content="Run" Command="{Binding RunSequenceCommand}"/>
            <Button Content="Stop" Command="{Binding SequenceStopCommand}"/>
            <Button Content="Live Start" Command="{Binding LiveStartCameraCommand}"/>
            <Button Content="Live Stop" Command="{Binding LiveStopCameraCommand}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Height="50">
                <TextBlock Text="Tool Run" Margin="20,0"/>
                <ToggleButton IsChecked="{Binding IsToolRun}"/>
            </StackPanel>
            <Image Grid.Column="2" Margin="10" Height="200" Source="{Binding GrabImage}" HorizontalAlignment="Stretch"/>
        </StackPanel>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>
        <ContentControl Grid.Column="2" Margin="3" Content="{Binding LaonDisplay}"/>
    </Grid>
</UserControl>
