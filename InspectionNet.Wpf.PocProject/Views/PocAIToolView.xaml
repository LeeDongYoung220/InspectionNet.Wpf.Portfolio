<UserControl x:Class="InspectionNet.Wpf.PocProject.Views.PocAIToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:InspectionNet.Wpf.PocProject.ViewModels"
             d:DataContext="{d:DesignInstance vm:PocAiToolViewModel}"
             DataContext="{Binding AiTool, Source={StaticResource ViewModelLocator}}"
             xmlns:local="clr-namespace:InspectionNet.Wpf.PocProject.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Load Model" Command="{Binding LoadTrainFileCommand}"/>
            <Button Content="Run" Command="{Binding AiRunCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
