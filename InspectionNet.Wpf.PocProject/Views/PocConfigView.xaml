<UserControl x:Class="InspectionNet.Wpf.PocProject.Views.PocConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:InspectionNet.Wpf.PocProject.ViewModels"
             d:DataContext="{d:DesignInstance vm:PocConfigViewModel}"
             DataContext="{Binding Config, Source={StaticResource ViewModelLocator}}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InspectionNet.Wpf.PocProject.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>

            <ToggleButton Grid.Column="0" IsChecked="{Binding SaveImage}" Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
            <TextBox Grid.Column="1" Margin="8" Padding="16,8" materialDesign:HintAssist.Hint="Save Path" Text="{Binding SaveImagePath}" IsEnabled="{Binding SaveImage}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            <Button Grid.Column="2" Margin="3" Padding="8" Command="{Binding SearchFolderCommand}" IsEnabled="{Binding SaveImage}" Style="{StaticResource MaterialDesignPaperButton}">
                <materialDesign:PackIcon Kind="FolderSearch"/>
            </Button>
        </Grid>
        
    </Grid>
</UserControl>
